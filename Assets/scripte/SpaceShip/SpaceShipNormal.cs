using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceShipNormal : SpaceShip
{
  
  /*  float speed = 4;
    public bool colliding = false;
    
    void Start()
    {
        // togo = new Transform() 
       
    }

    // Update is called once per frame
    void Update()
    {
        print("oooooooo");
        if (!colliding)
        {
            if (togo != null)
            {
              transform.rotation = Quaternion.LookRotation(Vector3.forward, togo.position);
              transform.position = Vector2.MoveTowards(transform.position, togo.position, speed * Time.deltaTime);
                
            }
           
        }
    }
    private void OnCollisionEnter2D(Collision2D collision)
    {
        if (collision.collider.tag == tag)
        {
            colliding = true;
        }
    }
    public void set_Tag(string tag)
    {
        this.tag = tag;
    }
    public void set_ToGo(Transform togo)
    {
        this.togo = togo;
    }
    */
}
